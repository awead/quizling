- id: "1"
  title: Label-free answer options in the backend schema
  description: >-
    Replace labeled options and correct_answer with is_correct options in the
    model, validation, loader, and API, per CAP-1, CAP-2 and the Constraints.
    Update backend tests and the docs that describe the question shape.
  spec_checkpoint: true
  done_checkpoint: true
  invoke_dev_with: >-
    The frontend and generator prompt still expect labels after this story;
    stories 2 and 3 fix them. Do not patch either here.
- id: "2"
  title: Generate questions in the new shape
  description: >-
    Update the generation prompt to emit is_correct options with no labels and
    explanations that never cite a letter or position, per CAP-4.
- id: "3"
  title: Frontend labels and quiz-mode shuffling
  description: >-
    Mirror the new schema in the frontend, shuffle options once per new quiz
    with letters assigned top to bottom, and grade by is_correct, per CAP-3.
    Browse and detail pages keep stored order.
